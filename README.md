# Face-Mask-Detector-with-CNN
Developed a Face Mask Detection system using deep learning with CNNs, trained on the Kaggle Face Mask dataset. Implemented data preprocessing, image resizing, normalization, model training, validation, and real-time prediction. Achieved over 92% test accuracy and built an interactive image-based mask detection interface.
